
#include "push_swap.h"

void ft_optim(t_type **head)
{
    ft_optim_PP(head);
    //ft_optim_SS(head);
    //ft_optim_RR(head);
    //ft_optim_RRR(head);
}

void ft_optim_PP(t_type **head)
{
    t_type *list;
    t_type *tail;
    int i;

    i = 0;
    list = (*head)->next;
    tail = list->next;
    while (!i)
    {






    }






}